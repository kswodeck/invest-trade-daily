__pycache__/
*.py[cod]
.venv/
venv/
env/
.env
.env.*
!.env.example

# Never let a service-account key land in git
*service-account*.json
*credentials*.json
gcp-key.json
client_secret*.json

.DS_Store
.pytest_cache/
*.egg-info/

# Cached SEC offer documents. They never change — an amendment is a new filing
# with its own accession — so the cache is pure speed and SEC politeness.
# actions/cache carries it between runs; a cold start costs nothing but time.
state/odd_lot_cache/
